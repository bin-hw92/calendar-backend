let Calendar,mongoose,Joi,sanitizeHtml;_64f‍.x([["getCalendarById",()=>getCalendarById],["getCalendarDay",()=>getCalendarDay],["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update],["checkOwnCalendar",()=>checkOwnCalendar]]);_64f‍.w("../../models/calendar",[["default",["Calendar"],function(v){Calendar=v}]]);_64f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_64f‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_64f‍.w("sanitize-html",[["default",["sanitizeHtml"],function(v){sanitizeHtml=v}]]);




const { ObjectId } = mongoose.Types;

const sanitizeOption = {
    allowedTags: [
        'h1',
        'h2',
        'b',
        'i',
        'u',
        's',
        'p',
        'ul',
        'ol',
        'li',
        'blockquote',
        'a',
        'img',
    ],
    allowedAttributes: {
        a: ['href', 'name', 'target'],
        img: ['src'],
        li: ['class'],
    },
    allowedSchemes: ['data', 'http'],
};

       const getCalendarById = async(ctx, next) => {
    const { id } = ctx.params;
    if(!ObjectId.isValid(id)){
        ctx.status = 400; //Bad Request
        return;
    }

    try{
        const calendar = await Calendar.findById(id);
        if(!calendar){
            ctx.status = 404; // Not Found
            return;
        }
        ctx.state.calendar = calendar;
        return next();                    
    }catch (e){
        ctx.throw(500, e);
    }
}

       const getCalendarDay = async(ctx, next) => {
    const { checkDate } = ctx.params;
    if(typeof checkDate !== 'string'){
        ctx.status = 400; //Bad Request
        return;
    }

    //$or, $and는 다들 아는 것
    //$gte:해당 값보다 크거나 같은 컬럼 값, 이상, $gt:해당 값보다 큰 컬럼 값, 초과 
    //$lte:해당 값보다 작거나 같은 컬럼 값, 이하, $lt:해당 값보다 작은 컬럼 값, 미만
    const query = {
        ...(checkDate ? {$or:[
            {$and :[
                {'startDay' : {'$lte': checkDate}},
                {'endDay' : {'$gte' : checkDate}},
                ]
            },
            {$or : [{'startDay' : checkDate},
                    {'endDay' : checkDate},
                ]
            }
        ]
    } : {}),
    };

    try{
        const calendar = await Calendar.find(query)
                                        .sort({ _id: -1})
                                        .lean()  // 해당 데이터를 JSON형태로 조회
                                        .exec();
        if(!calendar){
            ctx.status = 404; // Not Found
            return;
        }
        ctx.state.calendar = calendar;
        return next();                    
    }catch (e){
        ctx.throw(500, e);
    }
}

       const write = async ctx => {
    const schema = Joi.object().keys({
        // 객체가 다음 필드를 가지고 있음을 검증
        title: Joi.string().required(), // required()가 있으면 필수 항목
        body: Joi.string().required(),
        startDay: Joi.string().required(),
        startDate: {
            year: Joi.string().required(),
            month: Joi.string().required(),
            date: Joi.string().required(),
            hour: Joi.string().required(),
            min: Joi.string().required(),
        },
        endDay: Joi.string().required(),
        endDate: {
            year: Joi.string().required(),
            month: Joi.string().required(),
            date: Joi.string().required(),
            hour: Joi.string().required(),
            min: Joi.string().required(),
        },
        label: {
            style: Joi.string().required(),
            text: Joi.string().required(),
        }
    });
    // 검증하고 나서 검증 실패인 경우 에러 처리
    const result = schema.validate(ctx.request.body);
    if(result.error){
        ctx.status = 400; // Bad Request
        ctx.body = result.error;
        return;
    }

    const { title, body, startDay, startDate, endDay, endDate, label } = ctx.request.body;
    const calendar = new Calendar({
        title,
        body: sanitizeHtml(body, sanitizeOption),
        startDay,
        startDate,
        endDay,
        endDate,
        label,
        user: ctx.state.user,
    });
    
    try{
        await calendar.save();
        ctx.body = calendar;
    }catch(e){
        ctx.throw(500, e);
    }
};

// html을 업애고 내용이 너무 길면 200자로 제한하는 함수
const removeHtmlAndShorten = body => {
    const filtered = sanitizeHtml(body, {
        allowedTags: [],
    });
    return filtered.length < 200 ? filtered : `${filtered.slice(0, 200)}...`;
};

/*
    GET /api/calendar?year=&month=
*/
       const list = async ctx => {
    const year = ctx.query.year || ''+new Date().getFullYear;
    const month = ctx.query.month ||  ("0" + (1 + new Date().getMonth())).slice(-2);
    if(year.length < 1 || month.length < 1){
        ctx.status = 400;
        return;
    }

    const query = {
        ...(year && month ? {$or:[
                {$and : [{'startDate.year' : year},
                        {'startDate.month' : month},
                    ]
                },
                {$and : [{'endDate.year' : year},
                        {'endDate.month' : month},
                    ]
                }
            ]
        } : {}),
    };

    try{
        const calendar = await Calendar.find(query)
                        .sort({ _id: -1})
                        .lean()  // 해당 데이터를 JSON형태로 조회
                        .exec();
        const calendarCount = await Calendar.countDocuments(query).exec();
        //ctx.set('Last-Page', Math.ceil(postCount / 10));
        ctx.body = calendar.map(calendar => ({
                        ...calendar,
                        body: removeHtmlAndShorten(calendar.body),
                        daysize: calendar.startDay === calendar.endDay? 0 
                        : (new Date(calendar.endDay) - new Date(calendar.startDay)) / 1000 / 60 / 60 / 24, //같은 날짜면 0, 아니면 일 수 차이 반환
                    })); //길이가 200자 이상이면 자르기
    }catch (e){
        ctx.throw(500, e);
    }
};

       const read = async ctx => {
    ctx.body = ctx.state.calendar;
};

       const remove = async ctx => {
    const { id } = ctx.params;
    try{
        await Calendar.findByIdAndRemove(id).exec();
        ctx.status = 204; // No Content (성공하기는 했지만 응답할 데이터는 없음)
    }catch (e){
        ctx.throw(500, e);
    }
};

       const update = async ctx => {
    const { id } = ctx.params;
    // write에서 사용한 schema와 비슷한데, required()가 없습니다.
    const schema = Joi.object().keys({
        // 객체가 다음 필드를 가지고 있음을 검증
        title: Joi.string().required(), // required()가 있으면 필수 항목
        body: Joi.string().required(),
        startDay: Joi.string().required(),
        startDate: {
            year: Joi.string().required(),
            month: Joi.string().required(),
            date: Joi.string().required(),
            hour: Joi.string().required(),
            min: Joi.string().required(),
        },
        endDay: Joi.string().required(),
        endDate: {
            year: Joi.string().required(),
            month: Joi.string().required(),
            date: Joi.string().required(),
            hour: Joi.string().required(),
            min: Joi.string().required(),
        },
        label: {
            style: Joi.string().required(),
            text: Joi.string().required(),
        }
    });

    // 검증하고 나서 검증 실패인 경우 에러 차리
    const result = schema.validate(ctx.request.body);
    if(result.error){
        ctx.status = 400; // Bad Request
        ctx.body = result.error;
        return;
    }
    
    const nextData = {...ctx.request.body}; // 객체를 복사하고 body 값이 주어졌으면 HTML 필터링
    if(nextData.body){
        nextData.body = sanitizeHtml(nextData.body, sanitizeOption);
    }

    try{
        const calendar = await Calendar.findByIdAndUpdate(id, nextData, 
            {
                new: true, // 이 값을 설정하면 업데이트된 데이터를 반환합니다. 
                           // false일 때는 업데이트되기 전의 데이터를 반환합니다.
            }).exec();
            if(!calendar){
                ctx.status = 404;
                return;
            }
        ctx.body = calendar;
    }catch (e){
        ctx.throw(500, e);
    }
};

       const checkOwnCalendar = (ctx, next) => {
    const {user, calendar} = ctx.state;
    if(calendar.user._id.toString() !== user._id) { //포스트 등록자와 현재 사용자 동일 여부 체크
        ctx.status = 403;
        return;
    }
    return next();
}
